# Copy to scripts/e2e/.env (gitignored) and fill in the dev gateway admin login.
# Used ONLY to auto-reset the Perspective 2-hour trial. Never commit real values.
GATEWAY_USER=admin
GATEWAY_PASS=changeme
