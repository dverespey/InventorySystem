# Cutover Punch-List

The single tracked home for deferred-but-not-dropped items that must be resolved (or consciously accepted)
before the production cutover. Items are deferred on purpose — they are architect / cutover concerns, not
build blockers — but each one needs an owner-review so none silently rots. Created 2026-06-21 (team retro
R13/R19) to give the accumulating carries a home.

- **Owner:** David.
- **Review cadence:** at each milestone boundary (a milestone closes → walk this list, resolve / re-defer /
  escalate each OPEN item before opening the next milestone).
- **Status legend:** OPEN (carried), DECIDED (resolution recorded — keep until applied), DONE (applied to
  prod / closed).
- Each entry: **source unit** · one-line description · status. Detail lives in the cited per-unit doc /
  decision ledger; this is the index, not the spec.

| # | Source unit | Item | Status |
|---|---|---|---|
| P1 | EDI 862 (release) | **DECIDED (David 2026-06-22): report-only for now** (matches legacy — the 862 DELJIT firm stays a side report, touches no forecast/order table). Persistence design captured in `edi/inbound/862-firm-persistence-design.md` so we can pivot to persisting firm releases (planning-vs-firm) later WITHOUT rework. | DECIDED |
| P2 | M1 status render | **FORWARD-DEPENDENCY (assessed, not drainable now).** The inbound 997 AK9 `E`/`P` statuses are already STORED distinctly (`ak9_to_status` fixed the legacy A-vs-rest collapse + documents the requirement inline). But there is NO rebuilt ASN/INV status-DISPLAY NQ/view to add the `E`→"Accepted w/ errors" / `P`→"Partial" render arms to. The arms land WHEN that status-display screen is built (a daily-workflow UI not yet in scope) — don't fabricate a view. Data side done; render arms deferred to the status-display build. | DEFERRED |
| P3 | Order / GetShip | **DECIDED (David 2026-06-22): leave as-is.** This is the SUPPLIER ship-date (the order to sub-suppliers); the true supplier schedules are UNKNOWN, so the legacy guess is Mon-Fri + national holidays (`H`). It can't be made certain without real supplier calendars — the M-F+H behavior is the faithful, defensible default. The forecast day-spread honoring `O`/`X`/`W` is a different (TEMA production) calendar; the two legitimately differ. Accepted, not a defect. | DECIDED |
| P4 | Renban order | **DONE (PR #45 — David: FIX, varchar(3) is a spec requirement + a known trouble spot).** The legacy mis-wrap had ALREADY FIRED in prod (CMWA, Jan 2026: `999→1000→'100'`, re-issued `CMWA100/101`); CMWA sits at 863/1000 (freed only by ≥12mo DATAPURGE — no status frees a renban). FIXED two-part: (1) **clean wrap** `% 1000` (999→000, spec-compliant, wraps to the oldest/long-closed block; documented divergence D-RNB-1; oracle re-derived from spec §12.7 per R15); (2) a **collision-aware allocator (WARN→GUIDE→FIX** per `feedback-warn-guide-fix`): resident-rows exact-equality in-use predicate (self-safe), next-free RUN-of-N, an in-tx re-check on ALL commit paths; WARN=`RENBAN_COLLISION` alarm+home-hub, GUIDE=next-free run + colliding-release detail, FIX=use-next-free / override(confirm) / cancel; atomic. Builds the renban breakdown's first screen (`Order/RenbanBreakdown`+`RenbanCollisionDialog`) + a **server-side `auth.requireWrite` gate** (the new write). Dual-adversary-verified (2 BLOCKERs caught+fixed: a wrapped-run remap mis-issue + the ungated write; SF-1/2/3 fixed). Residual → P18. | DONE |
| P5 | Renban order | **DECIDED (decide-and-flag at build time): keep the safer all-lots-0 behavior.** The rebuild keeps the seed (no destructive counter clobber) when every order is sub-lot — a deliberate SAFER divergence from the legacy's destructive reset-to-1, recorded in the renban divergence ledger. Pathological (no live group parked at all-sub-lot). Retained here for cutover awareness only. | DECIDED |
| P6 | Forecast (.frc) | **DONE (PR #46 — David: IN USE, rebuild it).** Built the per-supplier forecast-DISTRIBUTION feed (`forecast_distribution/code.py`) — the outbound complement of the 830 import: reads `INV_BREAKDOWN_FC_INF` via `SELECT_ForecastSupplier` (future weeks, supplier-ordered) + per-supplier `SELECT_SupplierInfo`, emits the fixed-width `.frc` text (byte-faithful: `_format_qty` `%.Nd` min-width, `/`-only clean, CRLF) and/or the `.xls` Excel (M3 POI lane, fresh), TEXT/EXCEL/BOTH (NULL→BOTH), per-supplier all-or-nothing temp-then-rename (un-rename + true-state report, the P10 idiom), wired as the post-830-import non-fatal side-effect. The `SiteSupplierCode` crash fixed the single-site way (sendsite → `INV_SITES.VC_SUPPLIER_CODE`, restoring the legacy's commented-out `fiSupplierCode` + matching the `.ord`). Dual-adversary SHIP (query + .frc byte format == legacy exactly; emit/atomicity/POI/wiring sound). Golden-pending (P7): the exact sendsite byte/width + the Excel header titles. | DONE |
| P7 | Golden EDI / .ord | **CUTOVER-ONLY (M5-gated, cannot drain pre-cutover).** Golden-EDI/.ord byte-confirms: TEMA byte-parity; real INV_SITES DUNS/EIN/separator values; the delSL[4] inbound element index; the IT104 money scale; the .ord full-width; the 856 `y`-range (P13). These need real prod data / captured golden files — finalize at cutover. | OPEN (M5) |
| P8 | M4 (multi-site) | **MOSTLY DROPPED (David 2026-06-22): sites run on SEPARATE infra (own gateway+DB each) → single-site deployments, NOT shared-DB multi-tenancy.** The `site_id` schema surgery (site_id on 32 tables, the 7-trigger lockstep, the Class-B index swaps, the EIN-collision scoping of UPDATE_EINStatus/DELETE_AutoPurge) is UNNEEDED — each DB is isolated. The `-- M4` site_id markers in the code become moot. What REMAINS in M4: (1) ✅ DONE the single-site Sites master + INV_SITES path columns (PR #39); (2) minimal auth (Admin=user add/delete; production-control does everything else); (3) hardening (INI secrets→gateway config, backup runbook, single-site DATAPURGE). See `project-multisite` memory (reversed) + `m4-site-id-surgery-map.md` (superseded banner). | RESCOPED |
| P9 | Cutover (manifest cost) | **CUTOVER-ONLY (M5-gated).** Verify the `IX_INV_MANIFEST_COST_MST` constraint-DROP on PROD (the one residual constraint op from the cutover dress-rehearsal) — a prod op, done at cutover. | OPEN (M5) |
| P10 | Order file | **DONE (PR #44):** the order-file rollback path now reports the TRUE on-disk state (which destinations still hold a final `.ord` / which `.tmp` actually remain, FS-verified) instead of the blanket over-claim — accurate in both the clean-rollback and the rollback-of-rollback paths; non-vacuous test. | DONE |
| P11 | Test hygiene (R18) | **DONE (PR #44):** a shared `lib.preclean_sentinels` helper (error-swallowing, child-before-parent, sentinel-scoped) pre-cleans synthetic rows at suite START independent of a prior teardown error; wired into the seeding suites (forecast/supplier/master-CRUD/edi856/edi810/edi_inbound). Proven: a suite goes green on a DB left dirty by a simulated crashed predecessor. | DONE |
| P12 | Hot-call (HotCallEntry.pas) | **DONE (PR #37):** the `create_hotcall_asn` driver (reuses the M1 ASN seam, one atomic tx) + the Perspective hot-call entry view (headless shell; site-from-gateway; Designer-finish flagged) are built + dual-adversary-verified. Hot-call ASNs flow through the existing 856 as `8HC…`/M390. | DONE |
| P13 | EDI 856 (hot-call + normal filename) | **RESOLVED in code (2-part fix, operational-sender-faithful) — exact `y` range golden-pending at cutover.** Re-anchored from the RECREATE button (`ASNInvoice.pas:817-825`) to the OPERATIONAL SENDER `MainMenu.ResendMarkedEDIsClick` (`MainMenu.pas:2718/2723`, the live C→S-flip path). `_filename_856` now reproduces BOTH branches: NORMAL = `856 + MMDD(copy(pd,5,4)) + LineName + .txt` (`:2718`); HOT-CALL (`seq='-1'`) = `8HC + Y+MMDD(copy(pd,4,5)) + y + LineName + .txt` (`:2723`). BOTH were wrong before: both omitted `LineName`, and the merged NORMAL (PR #29) also had the wrong date offset. `y` reproduced per-ASN as `1 + count of same-day same-line hot-calls already 'S'`. **Remaining: confirm the exact `y` range/byte against a golden `8HC…` 856 at cutover.** | CODE-FIXED / golden-pending |
| P14 | Hot-call (minors) | **DONE (PR #37):** EIN-at-send applied to the hot-call driver; header `IN_QTY` now = the detail sum (fixed the legacy stale-`@QTY` garbage). | DONE |
| P15 | M4 auth (master CRUD writes) | **★ HIGH / CUTOVER-BLOCKER (M4 piece-2 systemic, adversarial-architect-reviewer).** The 7 other master CRUD views — **Size, Supplier, PartsStock, ManifestCost, RenbanGroup, AssemblyDetail, Logistics** — do New/Save/Delete via inline `runPrepUpdate`/INSERT/DELETE with **NO server-side role gate** (the same legacy H3 hole the Sites write just closed — worse, no gate at all). Supplier/PartsStock/ManifestCost feed billing + order math → an unauthenticated/forged-prop write is a **mis-bill / mis-order vector**, same class as the Sites BLOCKER. The reusable `auth.requireWrite(self.session)` gate IS built (PR #41 piece-2); each view is one call away. **MUST close before the prod flip** (sweep all 7 through `requireWrite`) — OR live-verify the Designer page-level role permissions for every master page. Do NOT flip cutover with these open. → **DONE (PR #42): all 7 swept** via a deterministic idempotent injector (`gen_master_write_gates.py`); both DB-write paths (Save+Delete) of every view server-gated before the write; dual-adversary-verified (every write path enumerated, forged-prop/anon rejected, live gateway deny-log evidence). The cutover-blocker is cleared. | DONE |
| P16 | M4 auth (master CRUD test coverage) | **Test-coverage hygiene (post-P15, non-security).** The P15 auth gate now denies the per-view CRUD tests' anon session, so `test_<view>_crud.py`'s **validation / delete-gate (refCount>0 blocked) / round-trip** assertions are SKIPPED — that coverage (incl. the CRITICAL D3 delete-gate blocked-branch) needs re-running under a seeded ProductionControl session (the gate logic + the CRUD logic are unchanged + were previously verified; only the test assertion lapsed). Plus a NIT: New is gated but Clear isn't (both are no-DB-write form-resets — gate both or neither). | **DONE (PR #43, M4 piece-3):** `test_master_crud_logic.py` re-covers validation + the D3 delete-gate-blocked branch + round-trip under a seeded ProductionControl session (22/0/1; all 6 gated masters proven, ManifestCost faithfully ungated); the Clear/New NIT fixed (both ungated — neither writes). The PartsStock `VehicleOrder.LINE` data-seed moved to P17. | DONE |
| P17 | M4 test-harness hygiene | **Non-blocking follow-ons (non-prod) from the piece-3 review.** (1) `jython_shim._bind` skips `?` in `--` comments + string literals but NOT `/* */` block comments — 7 `recordId.onChange` dropdown loaders (PartsStock/Supplier) carry `/* IG-SITE … ? */`, so shim-driving them IndexErrors (real JDBC skips block comments → not a prod bug, not a current regression; latent test-harness fidelity gap). Extend `_bind` to skip `/* */` + fix its docstring. (2) The delete-gate-blocked test SKIPs (not FAILs) if a gated master has no referenced row at runtime (not masking anything today — all 6 have refs) → fail-close it so a GREEN run proves all 6 ran. (3) PartsStock detail-load needs `VehicleOrder.dbo.LINE` seeded beyond COROLLA (read-side data-seed; VehicleOrder is RO on the spike). → **DONE (PR #44):** `_bind` now skips `/* */` block comments (12-case proven, cross-section green); the delete-gate-blocked test FAIL-closes (all 6 gated masters proven to run, +a reversible Logistics seed for a clean DB). **(3) residual:** the spike `VehicleOrder` is now a FULLY-RESTORED real RO DB (not the old stub) with `Line`=COROLLA only — the PartsStock multi-line dropdown coverage is a remaining **data-seed carry** (needs a real multi-line VehicleOrder restore; the old flat line-fixture is now guarded/superseded). The old stub→real-DB swap is captured. | DONE (residual: multi-line VehicleOrder data-seed) |
| P18 | Renban collision (residuals) | **Low-priority residuals from P4 (PR #45), non-blocking.** (1) **SF-3 TOCTOU window:** the in-tx renban re-check now runs on the tx connection, but a sub-ms window remains (no row lock / no unique constraint — a plain unique index is WRONG since renbans legitimately repeat). ACCEPTED + documented in-code (renban/code.py:516-518): the only ring-sharing concurrent writer (the Order-commit counter renban) is structurally absent in current data + near-single-operator. Optional belt: a cheap post-commit duplicate-renban tripwire on the alarm surface; revisit a non-unique covering index + `UPDLOCK,HOLDLOCK` only if concurrency rises. (2) The `Order/RenbanBreakdown`+`RenbanCollisionDialog` **Designer-finish items** (NQ data.bin XML, the `/order/renban` page-route mount, popup registration, bidirectional-binding-in-binding.config) — the standard headless-authoring carries, flagged. | OPEN (low) |
| P19 | Deploy config (DB connection) | **Cutover-deploy item (from the project rename).** The Ignition PROJECT was renamed `spike`→`InventorySystem` (display "Inventory System"), but the gateway **DB connection** is still named `Inventory_Spike` (every driver hardcodes `DATABASE="Inventory_Spike"`). At cutover, EITHER name the prod gateway's DB connection `Inventory_Spike` (so the drivers need no change) OR rename the connection + update every driver's `DATABASE=` constant in lockstep (+ a config indirection would be cleaner). Deploy-config only; no app-logic change. | OPEN (M5) |
| P20 | Hot-call (auth gate) | **★ Server-side write gate on the hot-call create (whole-surface, R25/R26).** `HotCall/HotCallEntry` (built P12/#37, BEFORE the auth module + the P15 sweep) ships a REAL write — the "Create Hot-Call ASN" button posts `create_hotcall_asn` to a gateway message handler that writes the ASN. The nav pass MOUNTED `/hotcall`, so that write is now reachable, and its server-side `auth.requireWrite` gate is **unverified / likely absent** (it predates auth). Same H3 class as P15 — a reachable privileged write must be server-gated. **Verify + add `auth.requireWrite` to the `create_hotcall_asn` handler (+ the button) before cutover.** Route to adversarial-architect-reviewer when the hot-call feature is next reviewed. | OPEN |

## Notes

- P5 and P3 are paired with EDI/renban divergence ledgers (renban; `edi810-decisions.md` style D-810-1..5);
  P5 is a *decided* divergence (safer/no-op → decide-and-flag), retained here so it isn't lost at cutover.
- P7's golden-EDI confirmations are byte/value-exact checks that can only be finalized against real prod
  INV_SITES rows — flag, don't fabricate.
- P8 (site_id / M4) is intentionally a whole-milestone bucket; expect it to expand as M4 is scoped, then
  drain into M4 build items.
